include:
  - remote: 'https://gitlab.com/cscs-ci/recipes/-/raw/master/templates/v2/.cscs.yml'
  - remote: 'https://gitlab.com/cscs-ci/recipes/-/raw/master/templates/v2/.ci-ext.yml'

stages:
  - build
  - test

##
## BUILDS
##

.build_spack_common:
  tags: [rosa-k8s]
  image: quay.io/podman/stable:latest
  stage: build
  variables:
    GIT_SUBMODULE_STRATEGY: recursive
    SPACK_SHA: 08efe13efb627793fbe048542890ee85668c8100
    SPACK_DLAF_REPO: ./spack
  before_script:
    - podman login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    # Note: a tag can contain 0-9 A-Z a-z -_.
    - TAG_IMAGE=`echo ${BASE_IMAGE##*/} | sed 's/[:]//g'`
    - TAG_APTGET=`echo ${EXTRA_APTGET} | sha256sum - | head -c 6`
    - TAG_COMPILER=`echo ${COMPILER}_CXX${CXXSTD} | sed 's/[@]//g'`
    - TAG_DOCKERFILE=`sha256sum $BUILD_DOCKER_FILE | head -c 16`
    - TAG_SPACK=`echo $SPACK_SHA | head -c 8`
    - TAG_REPO=`find $SPACK_DLAF_REPO -type f -exec sha256sum {} \; | sha256sum - | head -c 16`
    - TAG_ENVIRONMENT=`sha256sum $SPACK_ENVIRONMENT | head -c 16`
    - TAG=${TAG_IMAGE}-${TAG_APTGET}-${TAG_COMPILER}-MKL${USE_MKL}-${TAG_DOCKERFILE}-${TAG_SPACK}-${TAG_REPO}-${TAG_ENVIRONMENT}
    - mount -t tmpfs none /var/tmp
    - mount -t tmpfs none /var/lib/containers
    - '(podman pull $BUILD_IMAGE:$TAG && podman tag $BUILD_IMAGE:$TAG $BUILD_IMAGE:latest)
       ||  podman build
           -t $BUILD_IMAGE:$TAG -t $BUILD_IMAGE:latest
           --format docker --layers --isolation chroot
           --cache-from=$CI_REGISTRY_IMAGE/podman_cache --cache-to=$CI_REGISTRY_IMAGE/podman_cache
           --build-arg BASE_IMAGE
           --build-arg BUILDKIT_INLINE_CACHE=1
           --build-arg SPACK_SHA
           --build-arg EXTRA_APTGET
           --build-arg COMPILER
           --build-arg CXXSTD
           --build-arg SPACK_ENVIRONMENT
           --build-arg SPACK_DLAF_REPO --build-arg USE_MKL
           --build-arg NUM_PROCS=$(echo "$(cat /sys/fs/cgroup/cpu/cpu.cfs_quota_us) $(cat /sys/fs/cgroup/cpu/cpu.cfs_period_us) $(nproc)" | awk ''{ print ($1/$2 > 0) ? int($1/$2+0.5) : $3; }'')
           -f $BUILD_DOCKER_FILE .'

    - podman push $BUILD_IMAGE:$TAG
    - podman push $BUILD_IMAGE:latest
    - 'podman build
      -t $DEPLOY_IMAGE
      --format docker --layers --isolation chroot
      --cache-from=$CI_REGISTRY_IMAGE/podman_cache --cache-to=$CI_REGISTRY_IMAGE/podman_cache
      --build-arg BUILD_IMAGE=$BUILD_IMAGE:$TAG
      --build-arg DEPLOY_BASE_IMAGE
      --build-arg EXTRA_APTGET_DEPLOY
      --build-arg USE_MKL
      --build-arg USE_ROCBLAS
      --build-arg NUM_PROCS=$(echo "$(cat /sys/fs/cgroup/cpu/cpu.cfs_quota_us) $(cat /sys/fs/cgroup/cpu/cpu.cfs_period_us) $(nproc)" | awk ''{ print ($1/$2 > 0) ? int($1/$2+0.5) : $3; }'')
      -f $DEPLOY_DOCKER_FILE .'
    - podman push $DEPLOY_IMAGE
    - podman run -v $PWD/ci/ctest_to_gitlab.sh:/ctest_to_gitlab.sh $DEPLOY_IMAGE /ctest_to_gitlab.sh "$DEPLOY_IMAGE" "$USE_CODECOV" "$THREADS_PER_NODE" "$SLURM_CONSTRAINT" > pipeline.yml
  artifacts:
    paths:
      - pipeline.yml

# Builds a Docker image for the current commit
cpu release build gcc11:
  extends: .build_spack_common
  variables:
    BUILD_DOCKER_FILE: ci/docker/build.Dockerfile
    DEPLOY_DOCKER_FILE: ci/docker/deploy.Dockerfile
    BASE_IMAGE: docker.io/ubuntu:22.04
    DEPLOY_BASE_IMAGE: docker.io/ubuntu:22.04
    EXTRA_APTGET: ""
    EXTRA_APTGET_DEPLOY: "glibc-tools"
    # glibc-tools is needed for libSegFault on ubuntu:22.04
    COMPILER: gcc@11.2.0
    CXXSTD: 17
    USE_MKL: "ON"
    USE_ROCBLAS: "OFF"
    SPACK_ENVIRONMENT: ci/docker/cpu-release.yaml
    BUILD_IMAGE: $CI_REGISTRY_IMAGE/release-cpu-gcc11/build
    DEPLOY_IMAGE: $CI_REGISTRY_IMAGE/release-cpu-gcc11/deploy:$CI_COMMIT_SHA
    SLURM_CONSTRAINT: mc
    THREADS_PER_NODE: 72
    USE_CODECOV: "false"

cpu release build clang12:
  extends: .build_spack_common
  variables:
    BUILD_DOCKER_FILE: ci/docker/build.Dockerfile
    DEPLOY_DOCKER_FILE: ci/docker/deploy.Dockerfile
    BASE_IMAGE: docker.io/ubuntu:22.04
    DEPLOY_BASE_IMAGE: docker.io/ubuntu:22.04
    EXTRA_APTGET: clang-12
    EXTRA_APTGET_DEPLOY: "glibc-tools"
    # glibc-tools is needed for libSegFault on ubuntu:22.04
    COMPILER: clang@12.0.1
    CXXSTD: 17
    USE_MKL: "ON"
    USE_ROCBLAS: "OFF"
    SPACK_ENVIRONMENT: ci/docker/cpu-release.yaml
    BUILD_IMAGE: $CI_REGISTRY_IMAGE/release-cpu-clang12/build
    DEPLOY_IMAGE: $CI_REGISTRY_IMAGE/release-cpu-clang12/deploy:$CI_COMMIT_SHA
    SLURM_CONSTRAINT: mc
    THREADS_PER_NODE: 72
    USE_CODECOV: "false"

cpu release build clang13 cxx20:
  extends: .build_spack_common
  variables:
    BUILD_DOCKER_FILE: ci/docker/build.Dockerfile
    DEPLOY_DOCKER_FILE: ci/docker/deploy.Dockerfile
    BASE_IMAGE: docker.io/ubuntu:22.04
    DEPLOY_BASE_IMAGE: docker.io/ubuntu:22.04
    EXTRA_APTGET: clang-13
    EXTRA_APTGET_DEPLOY: "glibc-tools"
    # glibc-tools is needed for libSegFault on ubuntu:22.04
    COMPILER: clang@13.0.1
    CXXSTD: 20
    USE_MKL: "ON"
    USE_ROCBLAS: "OFF"
    SPACK_ENVIRONMENT: ci/docker/cpu-release.yaml
    BUILD_IMAGE: $CI_REGISTRY_IMAGE/release-cpu-clang13-20/build
    DEPLOY_IMAGE: $CI_REGISTRY_IMAGE/release-cpu-clang13-20/deploy:$CI_COMMIT_SHA
    SLURM_CONSTRAINT: mc
    THREADS_PER_NODE: 72
    USE_CODECOV: "false"

cpu codecov build gcc11:
  extends: .build_spack_common
  variables:
    BUILD_DOCKER_FILE: ci/docker/build.Dockerfile
    DEPLOY_DOCKER_FILE: ci/docker/codecov.Dockerfile
    BASE_IMAGE: docker.io/ubuntu:22.04
    DEPLOY_BASE_IMAGE: docker.io/ubuntu:22.04
    EXTRA_APTGET: ""
    EXTRA_APTGET_DEPLOY: "glibc-tools"
    # glibc-tools is needed for libSegFault on ubuntu:22.04
    COMPILER: gcc@11.2.0
    CXXSTD: 17
    USE_MKL: "OFF"
    USE_ROCBLAS: "OFF"
    SPACK_ENVIRONMENT: ci/docker/cpu-debug.yaml
    BUILD_IMAGE: $CI_REGISTRY_IMAGE/codecov-cpu-gcc11/build
    DEPLOY_IMAGE: $CI_REGISTRY_IMAGE/codecov-cpu-gcc11/deploy:$CI_COMMIT_SHA
    SLURM_CONSTRAINT: mc
    THREADS_PER_NODE: 72
    USE_CODECOV: "true"

cuda release build gcc11:
  extends: .build_spack_common
  variables:
    BUILD_DOCKER_FILE: ci/docker/build.Dockerfile
    DEPLOY_DOCKER_FILE: ci/docker/deploy.Dockerfile
    BASE_IMAGE: docker.io/nvidia/cuda:11.7.0-devel-ubuntu22.04
    DEPLOY_BASE_IMAGE: docker.io/ubuntu:22.04
    EXTRA_APTGET: ""
    EXTRA_APTGET_DEPLOY: "glibc-tools"
    # glibc-tools is needed for libSegFault on ubuntu:22.04
    COMPILER: gcc@11.2.0
    CXXSTD: 17
    USE_MKL: "ON"
    USE_ROCBLAS: "OFF"
    SPACK_ENVIRONMENT: ci/docker/gpu-release.yaml
    BUILD_IMAGE: $CI_REGISTRY_IMAGE/release-cuda-gcc11/build
    DEPLOY_IMAGE: $CI_REGISTRY_IMAGE/release-cuda-gcc11/deploy:$CI_COMMIT_SHA
    SLURM_CONSTRAINT: gpu
    THREADS_PER_NODE: 24
    USE_CODECOV: "false"

cuda codecov build gcc11:
  extends: .build_spack_common
  variables:
    BUILD_DOCKER_FILE: ci/docker/build.Dockerfile
    DEPLOY_DOCKER_FILE: ci/docker/codecov.Dockerfile
    BASE_IMAGE: docker.io/nvidia/cuda:11.7.0-devel-ubuntu22.04
    DEPLOY_BASE_IMAGE: docker.io/ubuntu:22.04
    EXTRA_APTGET: ""
    EXTRA_APTGET_DEPLOY: "glibc-tools"
    # glibc-tools is needed for libSegFault on ubuntu:22.04
    COMPILER: gcc@11.2.0
    CXXSTD: 17
    USE_MKL: "OFF"
    USE_ROCBLAS: "OFF"
    SPACK_ENVIRONMENT: ci/docker/gpu-debug.yaml
    BUILD_IMAGE: $CI_REGISTRY_IMAGE/codecov-cuda-gcc11/build
    DEPLOY_IMAGE: $CI_REGISTRY_IMAGE/codecov-cuda-gcc11/deploy:$CI_COMMIT_SHA
    SLURM_CONSTRAINT: gpu
    THREADS_PER_NODE: 24
    USE_CODECOV: "true"

amdgpu release build clang10+rocm-5.2.3:
  extends: .build_spack_common
  variables:
    BUILD_DOCKER_FILE: ci/docker/build.Dockerfile
    DEPLOY_DOCKER_FILE: ci/docker/deploy-amdgpu.Dockerfile
    BASE_IMAGE: docker.io/rocm/dev-ubuntu-20.04:5.2.3
    DEPLOY_BASE_IMAGE: docker.io/rocm/dev-ubuntu-20.04:5.2.3
    EXTRA_APTGET: "clang hipblas hipblas-dev rocblas rocblas-dev rocsolver rocsolver-dev llvm-amdgpu"
    EXTRA_APTGET_DEPLOY: ""
    COMPILER: clang@10.0.0
    CXXSTD: 17
    USE_MKL: "OFF"
    USE_ROCBLAS: "ON"
    SPACK_ENVIRONMENT: ci/docker/amdgpu-release-rocm523.yaml
    BUILD_IMAGE: $CI_REGISTRY_IMAGE/release-amdgpu-clang10/build
    DEPLOY_IMAGE: $CI_REGISTRY_IMAGE/release-amdgpu-clang10/deploy:$CI_COMMIT_SHA
    SLURM_CONSTRAINT: mc
    THREADS_PER_NODE: 64
    USE_CODECOV: "false"


##
## RUNS
##

.run_common:
  stage: test
  trigger:
    strategy: depend

cpu release test gcc11:
  extends: .run_common
  needs:
    - cpu release build gcc11
  trigger:
    include:
      - artifact: pipeline.yml
        job: cpu release build gcc11

cpu release test clang12:
  extends: .run_common
  needs:
    - cpu release build clang12
  trigger:
    include:
      - artifact: pipeline.yml
        job: cpu release build clang12

cpu release test clang13 cxx20:
  extends: .run_common
  needs:
    - cpu release build clang13 cxx20
  trigger:
    include:
      - artifact: pipeline.yml
        job: cpu release build clang13 cxx20

cpu codecov test gcc11:
  extends: .run_common
  needs:
    - cpu codecov build gcc11
  trigger:
    strategy: depend
    include:
      - artifact: pipeline.yml
        job: cpu codecov build gcc11

cuda release test gcc11:
  extends: .run_common
  needs:
    - cuda release build gcc11
  trigger:
    include:
      - artifact: pipeline.yml
        job: cuda release build gcc11

cuda codecov test gcc11:
  extends: .run_common
  needs:
    - cuda codecov build gcc11
  trigger:
    strategy: depend
    include:
      - artifact: pipeline.yml
        job: cuda codecov build gcc11
