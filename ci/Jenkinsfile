#!groovy

pipeline {
  agent {
    node {
      label 'ssl_daintvm1'
    }
  }
  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }
  }

  post {
    always {
      archiveArtifacts artifacts: '**/*.out.txt', fingerprint: true
    }
  }
}
