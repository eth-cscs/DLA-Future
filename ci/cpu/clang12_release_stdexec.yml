include:
  - local: 'ci/common-ci.yml'

cpu clang12 stdexec release deps:
  extends: .build_deps_common
  variables:
    EXTRA_APTGET: "clang-12 libomp-12-dev"
    COMPILER: clang@12
    CXXSTD: 20
    USE_MKL: "ON"
    SPACK_ENVIRONMENT: ci/docker/release-cpu-stdexec.yaml
    BUILD_IMAGE: $CSCS_REGISTRY_PATH/cpu-clang12-stdexec-release/build

cpu clang12 stdexec release build:
  extends:
    - .build_common
    - .build_for_daint-mc
  needs:
    - cpu clang12 stdexec release deps
  variables:
    DEPLOY_IMAGE: $CSCS_REGISTRY_PATH/cpu-clang12-stdexec-release/deploy:$CI_COMMIT_SHA

cpu clang12 stdexec release test:
  extends: .run_common
  needs:
    - cpu clang12 stdexec release build
  trigger:
    include:
      - artifact: pipeline.yml
        job: cpu clang12 stdexec release build
