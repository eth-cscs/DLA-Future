#
# Distributed Linear Algebra with Future (DLAF)
#
# Copyright (c) 2018-2024, ETH Zurich
# All rights reserved.
#
# Please, refer to the LICENSE file in the root directory.
# SPDX-License-Identifier: BSD-3-Clause
#

packages:
  all:
    target: [x86_64]
  # Set intel MKL as default blas, lapack and scalapack provider.
  # Can be overwritten in environments if needed.
  blas:
    require: 'intel-oneapi-mkl'
  lapack:
    require: 'intel-oneapi-mkl'
  scalapack:
    require: 'intel-oneapi-mkl'
  mpi:
    require: 'mpich'
  blaspp:
    variants:
      - '~cuda'
      - '~openmp'
      - '~rocm'
  intel-oneapi-mkl:
    require:
      - 'threads=openmp'
  openblas:
    variants:
      - 'threads=openmp'
  mpich:
    # Requirement for ABI compatibility on the test system
    require:
      - '@3.4.2'
    variants:
      - '~fortran'
      - '~libxml2'
  hwloc:
    variants:
      - '~libxml2'
  pika:
    require:
      # https://github.com/pika-org/pika/compare/async-rw-mutex-optimizations:
      '@git.ef9e0aa08ddc0231927579548fe7c351df073409=0.31.0'
      # pika@main before the above PR:
      # - '@git.2ffc93f3d55b139f1a317201b835190b29c828f8=0.31.0'
  git:
    # Force git as non-buildable to allow deprecated versions in environments
    # https://github.com/spack/spack/pull/30040
    buildable: false
